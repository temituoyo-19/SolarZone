const joinTheseGuys = document.getElementById('join')

// Add a click event listener to the button
joinTheseGuys.addEventListener('click', function() {

  alert('This feauture isnt available yet');
});
